apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: users
  namespace: knative-serverless
spec:
  template:
    spec:
      containers:
      - image: docker.io/wajrcs/users-knative
        env:
        - name: TARGET
          value: "Python Sample v1"
